/**
 * Lambda-specific functions for persistence
 */
// import { Receipt } from "../../../../persistence/src/model";

export function saveToKinesis(data: unknown): unknown {
  // persists the given model to the db
  return data;
}
